import {DARK_THEME, LIGHT_THEME, type ThemeType} from '@blog/components/theme/types';

export const mainThemes: ThemeType[] = [DARK_THEME, LIGHT_THEME];

export const themeDataKey = 'adorsys-gis-blog-theme';
